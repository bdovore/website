<?php $fiche = $view->getHelper('ficheAlbum'); ?>
<div class="cadre1">
    <?php if (isset($view->dbs_auteur)) { 
        foreach ($view->dbs_auteur->a_dataQuery as $auteur) { 
            echo $fiche->urlAuteur($auteur);
     ?>
    <Br>
    
    <?php }} ?>

</div>
